services:
  app:
    build: .
    container_name: emails-app
    ports:
      - "9099:9099"
    environment:
      TZ: America/Guayaquil
      SERVER_PORT: 9099
      DB_URL: jdbc:postgresql://172.30.10.4:5432/emails_db
      DB_USER: postgres
      DB_PASS: EpmapaT2025
    env_file:
      - .env
    networks:
      - epmapat-network

networks:
  epmapat-network:
    external: true
